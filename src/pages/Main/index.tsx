import { Layout } from "@components";
import { Content } from "@pages/Main/components";

const Main: React.FC = () => {
  return (
    <Layout>
      <Content />
    </Layout>
  );
};

export default Main;
